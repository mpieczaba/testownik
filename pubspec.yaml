name: testownik
description: Multi-platform revamp of the (in)famous Testownik application
publish_to: "none"
version: 0.0.0

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
